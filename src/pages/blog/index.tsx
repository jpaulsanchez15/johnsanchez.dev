/*
 * This will serve as the index for the blog pages. This will redirect
 * the user to the individual blog posts.
 * This can map through the file names of markdownFiles and then
 * create a component for each of them with the href being the name of the file.
 */

const BlogIndex = () => {};

export default BlogIndex;
